<!--pages/menus/menu.wxml-->
<view id="box">
  <view wx:for="{{surveyList}}" wx:key="{{index}}" bindtap="junmp"
  data-type="{{surveyList[index].type}}">
  <image  id="login"  src="{{item.url}}"></image>
  <view id="text">
  <text>{{item.text}}</text>
  </view>
  </view>
</view>
